name: Issue Planning Workflow

# ONLY trigger on issue label events - never on push/PR
on:
  issues:
    types: [labeled]

permissions:
  issues: write
  contents: read
  pull-requests: read

jobs:
  planning:
    name: Plan Implementation
    runs-on: [self-hosted, solops-local]
    if: |
      github.event.label.name == 'stage:plan' && 
      github.event.action == 'labeled' &&
      github.event.issue.user.type != 'Bot' &&
      github.repository_owner == github.actor
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Install Python dependencies
        run: |
          echo "📦 Installing Python dependencies for runner environment..."
          echo "Python version: $(python3 --version)"
          echo "Python path: $(which python3)"
          echo "Architecture: $(uname -m)"
          echo "Current working directory: $(pwd)"
          echo "PYTHONPATH will be: ${{ github.workspace }}/app:${{ github.workspace }}"
          
          # Try to import pydantic first to see current state
          echo "Testing current pydantic installation..."
          python3 -c "import pydantic; print('Current pydantic version:', pydantic.__version__)" || echo "Pydantic not available or broken"
          
          # Install dependencies
          python3 -m pip install --user -r requirements.txt --force-reinstall --no-cache-dir
          
          # Verify installation
          echo "Verifying pydantic installation..."
          python3 -c "import pydantic; print('Installed pydantic version:', pydantic.__version__)"
          echo "✅ Dependencies installed successfully"
      
      - name: Extract issue context
        id: context
        run: |
          echo "issue_id=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
          echo "issue_title=${{ github.event.issue.title }}" >> $GITHUB_OUTPUT
      
      - name: Validate issue context
        run: |
          echo "🔍 Validating issue context..."
          echo "Issue number: ${{ github.event.issue.number }}"
          echo "Issue author: ${{ github.event.issue.user.login }}"
          echo "Label added: ${{ github.event.label.name }}"
          echo "Action: ${{ github.event.action }}"
          echo "Repository owner: ${{ github.repository_owner }}"
          echo "Actor: ${{ github.actor }}"
          
          # Additional security validation
          if [[ "${{ github.event.issue.user.type }}" == "Bot" ]]; then
            echo "❌ Skipping workflow - issue created by bot"
            exit 1
          fi
          
          if [[ "${{ github.event.action }}" != "labeled" ]]; then
            echo "❌ Skipping workflow - not a label addition event"
            exit 1
          fi
          
          if [[ "${{ github.event.label.name }}" != "stage:plan" ]]; then
            echo "❌ Skipping workflow - incorrect label"
            exit 1
          fi
          
          echo "✅ Issue context validation passed"
      
      - name: Policy Gate Evaluation
        id: policy
        env:
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🛡️ Evaluating policy gate for planning stage..."
          echo "🔍 Environment:"
          echo "  PYTHONPATH: $PYTHONPATH"
          echo "  Working directory: $(pwd)"
          echo "  Issue ID: ${{ github.event.issue.number }}"
          
          # Run policy gate and capture both stdout and stderr
          echo "🚀 Running policy gate evaluation..."
          set +e  # Don't exit immediately on error
          python3 -u .github/scripts/policy_gate_evaluation.py evaluate-stage \
            --stage=plan \
            --issue-id=${{ github.event.issue.number }} \
            --output-format=json > policy_stdout.log 2> policy_stderr.log
          exit_code=$?
          set -e
          
          echo "📋 Policy gate exit code: $exit_code"
          
          # Always show what we captured
          echo "=== STDOUT ==="
          cat policy_stdout.log || echo "(empty)"
          
          echo ""
          echo "=== STDERR ==="
          cat policy_stderr.log || echo "(empty)"
          
          # Check if it failed
          if [ $exit_code -ne 0 ]; then
            echo ""
            echo "❌ Policy gate evaluation FAILED with exit code $exit_code"
            echo "Setting policy decision to 'block' due to evaluation error"
            
            # Capture error details for the issue comment
            error_details=$(cat policy_stderr.log 2>/dev/null || echo "No error details available")
            
            echo "POLICY_DECISION=block" >> $GITHUB_ENV
            echo "POLICY_REASON=Policy evaluation script failed: $error_details" >> $GITHUB_ENV
            echo "CONSTRUCTED_PROMPT=" >> $GITHUB_ENV
            
            exit 1
          fi
          
          # Extract JSON from stdout
          policy_json=$(cat policy_stdout.log)
          
          if [ -z "$policy_json" ]; then
            echo "❌ No output from policy gate"
            exit 1
          fi
          
          echo ""
          echo "📋 Policy JSON:"
          echo "$policy_json"
          
          # Parse JSON response using jq
          decision=$(echo "$policy_json" | jq -r '.decision')
          reason=$(echo "$policy_json" | jq -r '.reason')
          constructed_prompt=$(echo "$policy_json" | jq -r '.constructed_prompt // ""')
          
          # Set environment variables for subsequent steps
          echo "POLICY_DECISION=$decision" >> $GITHUB_ENV
          echo "POLICY_REASON=$reason" >> $GITHUB_ENV
          echo "CONSTRUCTED_PROMPT<<EOF" >> $GITHUB_ENV
          echo "$constructed_prompt" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          
          echo ""
          echo "✅ Policy decision: $decision"
          echo "✅ Policy reason: $reason"
          echo "✅ Prompt length: ${#constructed_prompt} characters"
      
      - name: Debug Policy Decision
        run: |
          echo "🔍 Debugging policy decision environment variables:"
          echo "POLICY_DECISION='$POLICY_DECISION'"
          echo "POLICY_REASON='$POLICY_REASON'"
          echo "CONSTRUCTED_PROMPT length: ${#CONSTRUCTED_PROMPT}"
          
          if [ "$POLICY_DECISION" = "allow" ]; then
            echo "✅ Policy allows execution"
          else
            echo "❌ Policy does not allow execution (decision: $POLICY_DECISION)"
          fi
      
      - name: Execute Planning with Claude CLI
        id: planning
        if: env.POLICY_DECISION == 'allow'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
          CLAUDE_CLIENT_TYPE: cli
          REPO_ROOT: ${{ github.workspace }}
          CLAUDE_CODE_SESSION_ACCESS_TOKEN: ${{ secrets.CLAUDE_CODE_SESSION_ACCESS_TOKEN }}
        run: |
          echo "📋 Starting repository-aware planning workflow for issue #${{ github.event.issue.number }}"
          echo "🔍 Environment check:"
          echo "  PYTHONPATH: $PYTHONPATH"
          echo "  REPO_ROOT: $REPO_ROOT"
          echo "  CLAUDE_CLIENT_TYPE: $CLAUDE_CLIENT_TYPE"
          echo "  Python version: $(python3 --version)"
          echo "  POLICY_DECISION: $POLICY_DECISION"
          
          # Test imports before running
          echo "🧪 Testing Python imports..."
          python3 -c "import sys; print('Python path:', sys.path)" || echo "Failed to check Python path"
          python3 -c "from workflow_engine import WorkflowEngine; print('✅ WorkflowEngine imported')" || echo "❌ Failed to import WorkflowEngine"
          python3 -c "from claude_client_factory import get_claude_client; print('✅ Claude client factory imported')" || echo "❌ Failed to import claude_client_factory"
          
          echo "🚀 Executing planning workflow..."
          set -e  # Exit on error
          python3 .github/scripts/functional_workflow_executor.py planning \
            --issue-id=${{ github.event.issue.number }} \
            --prompt="$CONSTRUCTED_PROMPT" 2>&1 | tee planning_output.log
          
          echo "✅ Planning workflow execution completed"
      
      - name: Check if planning step ran
        if: always()
        run: |
          echo "🔍 Checking if planning step executed..."
          if [ "${{ steps.planning.outcome }}" = "" ]; then
            echo "❌ Planning step was SKIPPED (likely due to policy decision)"
            echo "   POLICY_DECISION was: $POLICY_DECISION"
          elif [ "${{ steps.planning.outcome }}" = "success" ]; then
            echo "✅ Planning step completed successfully"
          elif [ "${{ steps.planning.outcome }}" = "failure" ]; then
            echo "❌ Planning step FAILED"
          else
            echo "⚠️ Planning step outcome: ${{ steps.planning.outcome }}"
          fi
      
      - name: Handle Policy Block
        if: env.POLICY_DECISION == 'block'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          echo "❌ Planning blocked by policy gate"
          
          python3 .github/scripts/workflow_transition.py progress \
            ${{ github.event.issue.number }} \
            "planning" \
            "blocked" \
            "Policy gate blocked: $POLICY_REASON"
          
          python3 .github/scripts/workflow_transition.py transition \
            ${{ github.event.issue.number }} \
            "stage:plan" \
            "stage:blocked" \
            "planning"
      
      - name: Handle Policy Review Required
        if: env.POLICY_DECISION == 'review_required'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          echo "⚠️ Planning requires human review"
          
          python3 .github/scripts/workflow_transition.py progress \
            ${{ github.event.issue.number }} \
            "planning" \
            "review_required" \
            "Human review required: $POLICY_REASON"
      
      - name: Handle successful planning
        if: steps.planning.outputs.success == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          # Transition to prioritization stage
          python3 .github/scripts/workflow_transition.py transition \
            ${{ github.event.issue.number }} \
            "stage:plan" \
            "stage:prioritize" \
            "planning"
      
      - name: Handle blocked planning
        if: steps.planning.outputs.blocked == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          python3 .github/scripts/workflow_transition.py transition \
            ${{ github.event.issue.number }} \
            "stage:plan" \
            "stage:blocked" \
            "planning"
          
          python3 .github/scripts/workflow_transition.py progress \
            ${{ github.event.issue.number }} \
            "planning" \
            "blocked" \
            "Planning analysis blocked: ${{ steps.planning.outputs.reason }}"
      
      - name: Handle review required
        if: steps.planning.outputs.review_required == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          python3 .github/scripts/workflow_transition.py progress \
            ${{ github.event.issue.number }} \
            "planning" \
            "review_required" \
            "Human review required: ${{ steps.planning.outputs.reason }}"
      
      - name: Handle planning failure
        if: failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}/app:${{ github.workspace }}
        run: |
          echo "❌ Planning workflow failed - adding detailed error comment to issue"
          
          # Capture any available logs
          ERROR_DETAILS="Planning Workflow Failed - Repository-aware workflow execution failed during implementation planning."
          
          if [ -f planning_output.log ]; then
            echo "📋 Planning output log found:"
            cat planning_output.log
            ERROR_DETAILS="$ERROR_DETAILS\n\nLast output:\n\`\`\`\n$(tail -20 planning_output.log)\n\`\`\`"
          else
            echo "⚠️ No planning output log found"
          fi
          
          # Create detailed error comment
          python3 .github/scripts/workflow_transition.py progress \
            ${{ github.event.issue.number }} \
            "planning" \
            "failed" \
            "$ERROR_DETAILS\n\nCheck GitHub Actions logs for details. Workflow Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          # Transition to blocked state
          python3 .github/scripts/workflow_transition.py transition \
            ${{ github.event.issue.number }} \
            "stage:plan" \
            "stage:blocked" \
            "planning"